--echo #
--echo # Test crash recovery with uncommitted transactions on indexed COMPLEX columns
--echo # with PARTITIONED table and VIRTUAL columns
--echo #

# Enable both partitioning and virtual columns for this test
let $partition = 1;
let $virtual = 1;

# Run the base recovery test with both features enabled
--source suite/villagesql/startup/t/recovery_complex_index.test
